
#include "menu.h"
#include "../junk.h"

void Menu::GetCursorPos(int& cx, int& cy)
{
	JUNK(cursorpos);
}

Menu::Menu(void)
{
	JUNK(menu);
}

void Menu::PaintTraverse(void)
{
	JUNK(paint);
}

Menu menu;
